// CodeWalk data for chapter 14. Excerpts from
// torch_xla/csrc/runtime/pjrt_computation_client.cpp at pytorch/xla commit
// 41398bf, trimmed where marked; nothing else altered. Notes name what each
// call hands across the PJRT boundary, and which implementation from the
// other two walks catches it.
export const walk = {
  id: 'torch-client',
  title: 'PjRtComputationClient, one round trip above the seam',
  lang: 'cpp' as const,
  code: "std::vector<ComputationClient::DataPtr> PjRtComputationClient::TransferToDevice(\n    absl::Span<const std::shared_ptr<const TensorSource>> tensors) {\n  // ... metrics and profiler scopes trimmed ...\n  std::vector<ComputationClient::DataPtr> datas;\n  datas.reserve(tensors.size());\n  int64_t total_size = 0;\n  for (auto& tensor : tensors) {\n    xla::PjRtDevice* pjrt_device = StringToPjRtDevice(tensor->device());\n\n    total_size += xla::ShapeUtil::ByteSizeOf(tensor->shape());\n\n    std::shared_ptr<xla::PjRtBuffer> buffer =\n        std::move(client_\n                      ->BufferFromHostBuffer(\n                          tensor->data(), tensor->primitive_type(),\n                          tensor->dimensions(), tensor->byte_strides(),\n                          xla::PjRtClient::HostBufferSemantics::\n                              kImmutableUntilTransferCompletes,\n                          [tensor]() { /* frees tensor */ },\n                          *pjrt_device->default_memory_space(),\n                          /*device_layout=*/nullptr)\n                      .value());\n\n    ComputationClient::DataPtr data =\n        std::make_shared<PjRtData>(tensor->device(), tensor->shape(), buffer);\n    datas.push_back(data);\n  }\n  OutboundDataMetric()->AddSample(total_size);\n  CreateDataHandlesCounter()->AddValue(datas.size());\n\n  return datas;\n}\nstd::vector<ComputationClient::ComputationPtr> PjRtComputationClient::Compile(\n    std::vector<ComputationClient::CompileInstance> instances) {\n  // ... metrics and profiler scopes trimmed ...\n  std::vector<ComputationClient::ComputationPtr> computations;\n  // ... an env-flag static trimmed ...\n  for (auto& instance : instances) {\n    xla::CompileOptions compile_options;\n    for (const auto& [name, value] : custom_compile_options_) {\n      compile_options.env_option_overrides.push_back({name, value});\n    }\n    // ... the collective-matmul option overrides trimmed ...\n    if (instance.is_sharded) {\n      // TODO(yeounoh) multi-host, multi-slice configurations\n      compile_options.executable_build_options.set_use_spmd_partitioning(true);\n      // ... the output-propagation option trimmed ...\n      int num_partitions = client_->device_count();\n      compile_options.executable_build_options.set_num_partitions(\n          num_partitions);\n      compile_options.executable_build_options.set_num_replicas(1);\n      // ... the tupling flag, then auto-SPMD options and logging trimmed ...\n      // TODO(244391366) verify this is correct for the collectives ops\n      xla::DeviceAssignment device_assignment(1, client_->device_count());\n      // DeviceAssignment values must be the PjRtDevice ID, so we need to\n      // unwind the global ordinal mapping.\n      for (const auto& [device_id, global_ordinal] : global_ordinals_) {\n        device_assignment(0, global_ordinal) = device_id;\n      }\n      compile_options.executable_build_options.set_device_assignment(\n          device_assignment);\n    } else {\n      // ... an argument-layout TODO trimmed ...\n      compile_options.executable_build_options.set_num_partitions(1);\n      compile_options.executable_build_options.set_num_replicas(\n          client_->device_count());\n      // ... the same tupling flag trimmed ...\n      xla::DeviceAssignment device_assignment(client_->device_count(), 1);\n      // ... the same unwind comment trimmed ...\n      for (const auto& [device_id, global_ordinal] : global_ordinals_) {\n        device_assignment(global_ordinal, 0) = device_id;\n      }\n      compile_options.executable_build_options.set_device_assignment(\n          device_assignment);\n    }\n    // ... a comment about raising a Python exception on compiler failure ...\n    std::unique_ptr<xla::PjRtLoadedExecutable> executable;\n    if (runtime::sys_util::GetEnvBool(\"XLA_STABLEHLO_COMPILE\", false)) {\n      // ... the HLO to StableHLO conversion and its CompileAndLoad trimmed ...\n    } else {\n      executable = util::RaisePythonValueErrorOnFailure([&] {\n        return fake_xla_compile_ ? fake_xla_compile_()\n                                 : client_->CompileAndLoad(instance.computation,\n                                                           compile_options);\n      });\n    }\n    // ... compiled-memory-stats logging trimmed ...\n    XLA_ASSIGN_OR_THROW(\n        const std::vector<std::shared_ptr<xla::HloModule>>& hlo_modules,\n        executable->GetHloModules());\n    // ... an unused entry-computation local trimmed ...\n    std::shared_ptr<PjRtComputation> pjrt_computation =\n        std::make_shared<PjRtComputation>(\n            std::move(xla::XlaComputation(hlo_modules[0]->ToProto())),\n            instance.devices, std::move(executable));\n\n    computations.push_back(pjrt_computation);\n    // ... a compile-handle counter trimmed ...\n  }\n\n  return computations;\n}\nabsl::StatusOr<std::vector<ComputationClient::DataPtr>>\nPjRtComputationClient::ExecuteReplicated(\n    const ComputationClient::Computation& computation,\n    absl::Span<const ComputationClient::DataPtr> arguments,\n    absl::Span<const std::string> devices,\n    const ExecuteReplicatedOptions& options) {\n  // Shared ownership of the timed section ensures that it will only get logged\n  // once both `ExecuteReplicated` and the async work in `Execute` are\n  // complete; a copy is held from the lambda that releases it when done.\n  auto timed =\n      std::make_shared<metrics::TimedSection>(ExecuteReplicatedMetric());\n  // ... profiler scope trimmed ...\n  const PjRtComputation& pjrt_computation =\n      dynamic_cast<const PjRtComputation&>(computation);\n\n  std::vector<std::vector<xla::PjRtBuffer*>> argument_handles(\n      devices.size(), std::vector<xla::PjRtBuffer*>(arguments.size()));\n  {\n    // ... a profiler scope, a counter, and the ParallelFor header trimmed ...\n          for (int32_t i = start; i < end; ++i) {\n            auto pjrt_data =\n                std::dynamic_pointer_cast<PjRtShardedData>(arguments[i]);\n            ABSL_CHECK_EQ(pjrt_data->shards.size(), devices.size())\n                << \"Expected one shard per device\";\n\n            for (int32_t d = 0; d < devices.size(); d++) {\n              std::shared_ptr<PjRtData> shard = pjrt_data->shards[d];\n              // ... device address checks trimmed ...\n              argument_handles[d][i] = shard->buffer.get();\n    // ... the loops close and the counter joins ...\n  }\n\n  xla::ExecuteOptions execute_options;\n  execute_options.untuple_result = options.explode_tuple;\n  execute_options.strict_shape_checking = true;\n  // ... multi-slice and callback-layout options trimmed ...\n  // Grab the shared lock and block the `WaitDeviceOps` until buffer is\n  // ready. Since this is the SPMD code path. There is no points to grab\n  // devices lock for every individual device.\n  // ... a lock-timing log line trimmed ...\n  auto op_tracker = operation_manager_.StartOperation(spmd_device_str);\n  // ... its matching Done line trimmed ...\n  std::optional<std::vector<xla::PjRtFuture<>>> returned_futures =\n      std::vector<xla::PjRtFuture<>>();\n  std::vector<std::vector<std::unique_ptr<xla::PjRtBuffer>>> results;\n  {\n    // ... a profiler scope trimmed ...\n    XLA_ASSIGN_OR_RETURN(results, pjrt_computation.executable->Execute(\n                                      std::move(argument_handles),\n                                      execute_options, returned_futures));\n\n    (*returned_futures)[0].OnReady(\n        std::move([timed, op_tracker = std::move(op_tracker)](\n                      absl::Status unused) mutable {\n          timed.reset();\n          TF_VLOG(3) << \"ExecuteReplicated returned_future->OnReady finished\";\n        }));\n  }\n\n  size_t num_outputs = results[0].size();\n  std::vector<ComputationClient::DataPtr> data_handles(num_outputs);\n  // ... output shapes and shardings resolved, then a ParallelFor over them ...\n            std::vector<std::shared_ptr<PjRtData>> shards(devices.size());\n            for (int32_t d = 0; d < devices.size(); d++) {\n              std::unique_ptr<xla::PjRtBuffer> buffer =\n                  std::move(results[d][i]);\n              shards[d] =\n                  std::make_shared<PjRtData>(devices[d], std::move(buffer));\n            }\n\n            data_handles[i] = std::make_shared<PjRtShardedData>(\n                spmd_device_str, output_shapes[i], std::move(shards),\n                output_shardings[i]);\n  // ... the pool joins ...\n  return data_handles;\n}\nabsl::StatusOr<std::vector<xla::Literal>>\nPjRtComputationClient::TransferFromDevice(absl::Span<const DataPtr> handles) {\n  // ... metrics and profiler scopes trimmed ...\n  std::vector<xla::PjRtFuture<>> futures;\n  futures.reserve(handles.size());\n  std::vector<xla::Literal> literals;\n  literals.reserve(handles.size());\n  int64_t total_size = 0;\n  for (auto handle : handles) {\n    // Use XLA replication to reassemble the sharded data. If input handle\n    // is not sharded, then it is a no-op.\n    std::shared_ptr<PjRtData> pjrt_data = ReplicateShardedData(handle);\n    ABSL_CHECK(pjrt_data) << \"PjRt_data is null in \" << __FUNCTION__;\n    ABSL_CHECK(pjrt_data->buffer != nullptr)\n        << \"PjRt buffer is null in \" << __FUNCTION__;\n\n    xla::Literal& literal = literals.emplace_back(\n        xla::Literal(host_output_shape(pjrt_data->buffer.get())));\n    futures.push_back(pjrt_data->buffer->ToLiteral(&literal));\n\n    total_size += literal.size_bytes();\n  }\n  XLA_RETURN_IF_ERROR(xla::JoinFutures(futures).Await());\n  InboundDataMetric()->AddSample(total_size);\n\n  return literals;\n}",
  steps: [
    { lines: [1, 23] as [number, number], note: "Parameters leave the host here, one tensor at a time. What crosses is a raw pointer plus the facts PJRT needs to read it: element type, dimensions, byte strides, and the memory space of the device that string named. The semantics argument is the promise about that pointer, kImmutableUntilTransferCompletes, so the runtime may read it until the copy finishes and the lambda then drops torch_xla's last reference to the tensor. The bytes were staged before this call, in AtenSource's constructor, which moved the at::Tensor to contiguous CPU memory in the target dtype. Underneath is the cpu-client walk's BufferFromHostBuffer, taking its copying branch rather than the zero-copy one, and a plugin catches the same call as PJRT_Client_BufferFromHostBuffer." },
    { lines: [24, 32] as [number, number], note: "What comes back is not memory. PjRtData pairs the device string, the xla::Shape the frontend already knew, and the buffer pointer, and that triple is what the LazyTensor core stores as opaque backend data. Nothing has been waited on anywhere in this function, because the buffer carries a definition event underneath and the handle is valid long before the bytes land. The IFRT adapter builds its own version of this object at the same point, wrapping per-device buffers into a PjRtArray. torch_xla wraps them into a type of its own instead, which is why the sharded path later needs a second one." },
    { lines: [33, 43] as [number, number], note: "Compilation crosses in batches, one CompileInstance per graph the executor wants built. A fresh options object is constructed per instance, and nothing of the frontend reaches the compiler except what gets written into it. First in are the custom compile options, the string pairs Python set through torch_xla.set_custom_compile_options, which land as env_option_overrides and arrive at the compiler as XLA flags. So the first thing to cross at compile time is a list of flag names, and the program follows later." },
    { lines: [44, 75] as [number, number], note: "One boolean picks between two device-assignment shapes. On the SPMD arm the module is compiled once for N partitions and a single replica, and the assignment is a 1-by-N row. On the other arm it is one partition and N replicas, the assignment is an N-by-1 column, and the same global-ordinal unwind fills it with the indices swapped. Both loops exist because torch_xla numbers devices in global ordinals of its own while PJRT wants PjRtDevice ids, so the mapping is inverted right here rather than anywhere else. The ifrt-adapter walk runs the identical translation in TranslateDeviceIds before delegating, which is the same reconciliation at a different altitude." },
    { lines: [76, 102] as [number, number], note: "The compile crossing itself hands over one XlaComputation, an HLO proto, plus the options built above. StableHLO only crosses when XLA_STABLEHLO_COMPILE is set, so by default torch_xla gives PJRT HLO where JAX gives it MLIR. On the far side, CompileAndLoad is the function the cpu-client walk opens with, the one that ends in RunHloPasses and RunBackend; a plugin sees it arrive as PJRT_Client_Compile. What comes back is unwrapped at once, because GetHloModules pulls the optimized module out so PjRtComputation can hold the post-optimization program next to the executable." },
    { lines: [103, 134] as [number, number], note: "Execution starts by turning the arguments inside out. torch_xla holds one sharded handle per argument, each carrying one shard per device, and PJRT wants one buffer list per device. The nested loop rewrites index [i][d] as [d][i] and drops to raw pointers, since the caller keeps ownership of every input across the call. The ifrt-adapter walk performs the same transpose one layer down for the same reason, so two independent layers above PJRT both found its argument shape to be the transpose of theirs. A thread pool does the rewriting only because a large model has thousands of parameters and the per-argument work was measured at ten microseconds." },
    { lines: [135, 161] as [number, number], note: "The lock is taken once, on a device that does not exist. spmd_device_str is the literal SPMD:0, a virtual device the client tracks so WaitDeviceOps can block on the whole SPMD program instead of on every real device in turn. StartOperation hands back an RAII tracker whose destructor marks the operation complete, and the callback moves that tracker into the future's OnReady lambda, so the count stays raised until PJRT reports the execution done. Read execute_options while it is on screen: untuple_result, strict shape checking, and nothing about donation, because the input-output alias was compiled into the module. Below the Execute call is the virtual the cpu-client walk ends inside, and a plugin sees PJRT_LoadedExecutable_Execute with this future arriving as a PJRT_Event." },
    { lines: [162, 178] as [number, number], note: "Results come back up as sharded handles, with the transpose run in reverse. results[d][i] is device d's copy of output i, so the inner loop collects one PjRtData per device and the outer wrap ties them to the output shape and the sharding the executable was compiled with. The device on the result is SPMD:0 again, the virtual one, which is how the frontend treats a program's output as a single tensor while its bytes sit on every chip. PjRtArray is what the IFRT adapter constructs at this exact point in its own Execute. torch_xla constructs PjRtShardedData instead, because PJRT returns buffers and something above it has to say which array they belong to." },
    { lines: [179, 205] as [number, number], note: "Here is the first genuinely blocking call in the round trip. ReplicateShardedData turns a sharded handle back into one buffer, and when the sharding is not replicated it does that by compiling and running a small identity program, so printing a tensor can cost a compile. Each buffer then gets a host Literal sized from its own device shape, ToLiteral starts the copy and returns a future, and only JoinFutures().Await() waits. Every read is issued before anything waits, the same shape the transfer up had. Underneath is the definition event the cpu-client walk introduced, and in a plugin this pair reads as PJRT_Buffer_ToHostBuffer followed by PJRT_Event_Await." },
  ],
}
