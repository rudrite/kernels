// CodeWalk data for the tiled matmul kernel. Code copied verbatim from
// labs/stage-1/lab-1.2-tiled-matmul.ipynb (matmul_kernel and its pallas_call
// wrapper, minus the notebook's own benchmark invocation below the call).
export const walk = {
  id: 'matmul',
  title: "tiled matmul: the reduction kernel and its pallas_call",
  lang: 'python' as const,
  code: "def matmul_kernel(a_ref, b_ref, o_ref):\n    k = pl.program_id(2)\n    @pl.when(k == 0)\n    def _init():\n        o_ref[...] = jnp.zeros_like(o_ref)\n    o_ref[...] += jnp.dot(a_ref[...], b_ref[...], preferred_element_type=jnp.float32).astype(o_ref.dtype)\n\ndef matmul(a, b, bm=128, bn=128, bk=128):\n    m, k = a.shape\n    _, n = b.shape\n    return pl.pallas_call(\n        matmul_kernel,\n        grid=(m // bm, n // bn, k // bk),\n        in_specs=[\n            pl.BlockSpec((bm, bk), lambda i, j, kk: (i, kk)),\n            pl.BlockSpec((bk, bn), lambda i, j, kk: (kk, j)),\n        ],\n        out_specs=pl.BlockSpec((bm, bn), lambda i, j, kk: (i, j)),\n        out_shape=jax.ShapeDtypeStruct((m, n), a.dtype),\n        interpret=INTERP,\n    )(a, b)",
  steps: [
    { lines: [0, 1], note: "The kernel takes three references, not two arrays. a_ref and b_ref hold the input blocks for this grid step; o_ref holds the block of the output this step owns. Pallas has already sliced the full A and B matrices down to a (bm, bk) and (bk, bn) tile before the call, and k = pl.program_id(2) reads which K-block this grid step landed on. That line is schedule: it says where in the reduction the kernel is, not what to compute." },
    { lines: [2, 4], note: "This runs a check on every grid step even though it only ever does something on the first one. When k is 0, the first K-block for the current output tile, the kernel zeroes the accumulator with jnp.zeros_like. On every later K-block the zeroing is skipped, so the accumulator keeps whatever the previous step left in it. Deciding when to reset is schedule; the reset itself is the one piece of algorithm needed to start a clean sum." },
    { lines: [5, 5], note: "One line carries the entire arithmetic. o_ref[...] += jnp.dot(...) multiplies the A and B tiles on the MXU and adds the result into the accumulator already sitting in o_ref. This is the algorithm in full: matrix multiply and accumulate, repeated once per K-block until every block for this output tile has been folded in." },
    { lines: [7, 9], note: "matmul() is the Python entry point a caller actually invokes. It unpacks the shapes of A and B and takes three block sizes, bm, bn, bk, each defaulting to 128. None of this touches TPU memory yet; it sets up the numbers pallas_call needs to carve the problem into tiles." },
    { lines: [10, 12], note: "grid=(m // bm, n // bn, k // bk) is the schedule in its most compressed form: three integers saying how many output-row tiles, output-column tiles, and reduction tiles the whole matmul breaks into. Pallas runs the kernel once for every point in that three-dimensional grid." },
    { lines: [13, 16], note: "in_specs tells Pallas which slice of A and B to hand the kernel at grid coordinate (i, j, kk). The lambda for A ignores j and returns (i, kk): row block i, K-block kk. The lambda for B ignores i and returns (kk, j). Neither lambda computes anything, which is what makes this schedule rather than algorithm." },
    { lines: [17, 20], note: "out_specs maps grid coordinate (i, j, kk) to output tile (i, j), so every K-block for the same (i, j) writes into the same physical o_ref location, which is exactly what lets the accumulation in the kernel work. out_shape and interpret set the result's shape, dtype, and whether the call runs in Python or compiles for the TPU, and the final (a, b) is the actual invocation." },
  ],
}
