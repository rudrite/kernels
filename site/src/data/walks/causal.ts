// CodeWalk data for the causal flash attention kernel. Code copied verbatim
// from labs/stage-3/lab-3.4-masks-as-loop-structure.ipynb
// (causal_flash_kernel, the whole cell as written).
export const walk = {
  id: 'causal',
  title: "causal masking: the loop bound as the mask",
  lang: 'python' as const,
  code: "import functools\n\ndef causal_flash_kernel(q_ref, k_ref, v_ref, o_ref, *, block_q, block_kv):\n    qi = pl.program_id(0)\n    q = q_ref[...].astype(jnp.float32)\n    row0 = qi * block_q\n\n    def step(j, state):\n        m, l, acc = state\n        kb = jax.lax.dynamic_slice_in_dim(k_ref[...], j * block_kv, block_kv).astype(jnp.float32)\n        vb = jax.lax.dynamic_slice_in_dim(v_ref[...], j * block_kv, block_kv).astype(jnp.float32)\n        s = q @ kb.T\n        cols = j * block_kv + jax.lax.broadcasted_iota(jnp.int32, s.shape, 1)\n        rows = row0 + jax.lax.broadcasted_iota(jnp.int32, s.shape, 0)\n        s = jnp.where(cols <= rows, s, -jnp.inf)      # edge block: mask inside\n        m_new = jnp.maximum(m, jnp.max(s, axis=-1))\n        alpha = jnp.exp(m - m_new)\n        p = jnp.exp(s - m_new[:, None])\n        l_new = l * alpha + jnp.sum(p, axis=-1)\n        acc_new = acc * alpha[:, None] + p @ vb\n        return m_new, l_new, acc_new\n\n    # the causal structure IS the loop bound: blocks past the diagonal never run\n    n_live = (row0 + block_q + block_kv - 1) // block_kv\n    m0 = jnp.full((block_q,), -jnp.inf, jnp.float32)\n    l0 = jnp.zeros((block_q,), jnp.float32)\n    acc0 = jnp.zeros((block_q, v_ref.shape[1]), jnp.float32)\n    m, l, acc = jax.lax.fori_loop(0, n_live, step, (m0, l0, acc0))\n    o_ref[...] = (acc / l[:, None]).astype(o_ref.dtype)",
  steps: [
    { lines: [0, 2], note: "causal_flash_kernel takes the same four refs as flash_kernel plus one more static argument: block_q joins block_kv, because this kernel now needs to know both how wide a query tile is and how wide a key/value chunk is. Wiring the arguments through functools.partial and BlockSpecs works exactly as it did for plain flash attention; what changes is what happens inside." },
    { lines: [3, 5], note: "qi = pl.program_id(0) reads which query-block grid step this call is, the same role k played across the K-blocks in the matmul kernel. row0 converts that step index into an actual row number, qi times block_q, the first global row this query tile covers. That number is schedule information the mask below needs: it has to know where these rows sit in the full sequence, not just which tile they belong to." },
    { lines: [7, 11], note: "step still slices out chunk j of K and V and computes s = q @ kb.T exactly as it did in plain flash attention. Nothing here yet distinguishes causal attention from full attention; the difference is about to appear in the scores this line produces, not in how they're computed." },
    { lines: [12, 14], note: "cols and rows reconstruct, for every entry of the score matrix, which absolute key position and which absolute query position it belongs to. jnp.where then keeps a score only where the key column is at or before the query row, cols <= rows, and sets everything else to negative infinity. That's the causal mask, and it's algorithm: it decides which query-key pairs are allowed to matter at all, before softmax runs." },
    { lines: [15, 20], note: "From here the recurrence is identical to plain flash attention: track the running max, rescale by alpha, accumulate the weighted sum. The masked-out entries carry an exp of negative infinity, which is zero, so they vanish from the sum without any special-case code in this part." },
    { lines: [22, 23], note: "This is where causal attention earns its speed. n_live shrinks the loop bound itself so that key/value chunks strictly past the diagonal never get visited at all, not even to be masked out. Where flash_kernel always looped over every chunk, this kernel loops over fewer chunks as row0 grows, and that's schedule: fewer iterations, not less work per iteration." },
    { lines: [24, 28], note: "State initialization, the fori_loop call, and the final divide-and-write are the same three moves as plain flash attention, just parameterized by n_live instead of a fixed chunk count. The output for each query row still ends up correctly normalized, because every chunk that mattered for that row was visited and every chunk that couldn't have mattered was skipped by the loop bound, not by wasted computation." },
  ],
}
